{
    "reviews": [
        {
            "body": "All necessary `using` directives and the `namespace` declaration have been removed. This will cause compilation errors since the code references types like `Serilog`, `RepoProvider`, `InstructionsProvider`, etc. that require these imports. Was this intentional?",
            "line": ""
        },
        {
            "body": "The class declaration and method signature have been collapsed onto a single line (`public class Program {    public static void Main(string[] args) {`). This is a formatting issue — the method signature should be on its own line.",
            "line": "public class Program {    public static void Main(string[] args) {"
        },
        {
            "body": "This line is clearly not valid C# code. It looks like debug/test gibberish that was accidentally left in. This will cause a compilation error and must be removed.",
            "line": "        CALL FUNCTION_RUN FACT CHECK RUN BACK... RUN"
        },
        {
            "body": "This is an HTML comment (`<!-- REMOVE -->`) which is not valid C# syntax. This will cause a compilation error and must be removed.",
            "line": "        <!-- REMOVE -->"
        },
        {
            "body": "This line is truncated — `ValidateInstructionsPr` is incomplete (should be `ValidateInstructionsProvider(_instructionsProvider);`). This will not compile.",
            "line": "        InstructionsProvider instructionsProvider = ValidateInstructionsPr"
        },
        {
            "body": "The `_chatProvider` configuration block and `ValidateChatProvider` call have been removed. If the chat provider is still needed by downstream services, this will cause a runtime failure. Was this removal intentional, or was it accidental?",
            "line": "        InstructionsProvider instructionsProvider = ValidateInstructionsPr"
        }
    ]
}